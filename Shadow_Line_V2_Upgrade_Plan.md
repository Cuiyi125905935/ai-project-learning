# 上影线模型 2.0 升级方案：从“偏科”到“全能”

## 1. 现状诊断：1.0 版本的局限性
通过对 `technical_analysis_platform/scripts/train_shadow_ml_v2.py` 的深度分析，我们发现当前模型存在明显的**“特征偏科”**问题：
*   **过度依赖量价因子**：目前仅使用了 `SYX0` (上影线长度)、`量比(5日)`、`当日涨幅` 和 `影实比`。
*   **形态结构缺失**：模型完全忽略了 K 线的**空间位置**（如是否处于支撑位）和**组合形态**（如是否为“仙人指路”或“射击之星”）。
*   **缺乏自我迭代**：目前的训练是静态的，没有引入“失败案例回溯”机制，导致模型无法从每日的错误中自动学习。

---

## 2. 仓库借鉴：从开源项目中提取“进化基因”
结合我们 `ai-project-learning` 仓库中的深度分析报告，我们可以引入以下先进理念进行 2.0 升级：

| 借鉴来源 | 核心逻辑 | 在上影线 2.0 中的应用 |
| :--- | :--- | :--- |
| **HQChart** | **动态角度校准** | 引入江恩 1x1 线斜率，判断上影线是否突破了关键压力位。 |
| **Stock-Pattern-Analyzer** | **量能周期匹配** | 增加“量能健康度”评分，确保上影线出现在量能爆发的初期而非末期。 |
| **pmdarima** | **时间周期共振** | 利用 ARIMA 识别个股的变盘时间窗，只在高概率变盘点触发信号。 |
| **LightGBM (FinRL)** | **特征重要性筛选** | 引入更多 K 线形态因子（如实体比例、下影线支撑），让模型自动筛选有效特征。 |

---

## 3. 2.0 版本核心升级点：K 线形态与自我迭代

### 一、形态结构补全（解决“不学 K 线”的问题）
我们将新增以下**形态感知特征**，让模型学会识别单根及组合 K 线结构：
1.  **相对位置因子 (`Position_Ratio`)**：`(Close - Low_60) / (High_60 - Low_60)`。识别上影线是出现在底部（试盘）还是顶部（出货）。
2.  **下影线支撑因子 (`Lower_Shadow_Support`)**：如果存在下影线，计算其长度占比，判断下方是否有承接力。
3.  **多日形态因子 (`Pattern_3D`)**：对比前两根 K 线的收盘价，识别“红三兵后长上影”或“跳空高开长上影”等经典结构。

### 二、自我学习与迭代机制（实现“每天出票”的稳定性）
为了达到 **成功率 > 63%** 且 **涨幅 > 3.5%** 的目标，我们引入**在线学习 (Online Learning)** 流程：
1.  **每日复盘**：每天收盘后，自动将当日的“预测结果”与“实际涨跌”进行比对。
2.  **增量更新**：使用 LightGBM 的 `init_model` 参数，在旧模型基础上利用新产生的标注数据进行微调（Fine-tuning）。
3.  **动态阈值**：根据最近 5 天的市场情绪（如涨停家数），动态调整输出信号的置信度阈值。

---

## 4. 最终输出命令与目标
**目标指令**：`python pipeline/shadow_line_v2_predictor.py --daily_scan --target_success_rate 0.63`

**输出标准**：
*   **频率**：每日收盘后自动运行一次。
*   **数量**：精选 2-3 支最具爆发潜力的个股。
*   **指标**：
    *   **综合得分**：融合量价、时空、形态的四维共振分（满分 100）。
    *   **预期涨幅**：基于历史相似形态回测的平均次日收益。
    *   **成功率预估**：模型给出的概率值需严格大于 63%。

---

## 5. 实施路线图
1.  **第一步**：在 `train_shadow_ml_v2.py` 基础上，增加 K 线形态特征工程模块。
2.  **第二步**：整合 `ai-project-learning` 中的 VPFE 参数，优化量能过滤逻辑。
3.  **第三步**：编写自动化迭代脚本，实现模型的“日更”能力。

**结论**：通过引入仓库中已验证的江恩时空逻辑和形态学因子，上影线 2.0 将从一个简单的“涨跌幅预测器”进化为一个具备**自我修复能力的形态识别专家**。
